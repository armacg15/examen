<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <title>Editar Alumno</title>
</head>

<body>
    <div class="container mt-5">
        <h1 class="text-center">Editar Alumno</h1>
        <form action="/alumnos/<%= alumno._id %>?_method=PUT" method="POST">
            <div class="form-group">
                <label for="NC">Número de Control</label>
                <input type="number" class="form-control" id="NC" name="NC" value="<%= alumno.NC %>" required>
            </div>
            <div class="form-group">
                <label for="Nombre">Nombre</label>
                <input type="text" class="form-control" id="Nombre" name="Nombre" value="<%= alumno.Nombre %>" required>
            </div>
            <div class="form-group">
                <label for="ApellidoPaterno">Apellido Paterno</label>
                <input type="text" class="form-control" id="ApellidoPaterno" name="ApellidoPaterno" value="<%= alumno.App || '' %>">
            </div>
            <div class="form-group">
                <label for="ApellidoMaterno">Apellido Materno</label>
                <input type="text" class="form-control" id="ApellidoMaterno" name="ApellidoMaterno" value="<%= alumno.Apm || '' %>">
            </div>
            <div class="form-group">
                <label for="IDGrupo">ID de Grupo</label>
                <input type="text" class="form-control" id="IDGrupo" name="IDGrupo" value="<%= alumno.IDGrupo || '' %>">
            </div>
            <button type="submit" class="btn btn-success">Guardar Cambios</button>
            <a href="/alumnos" class="btn btn-danger">Cancelar</a>
        </form>
        <!-- Botón de Eliminar -->
        <form action="/alumnos/<%= alumno._id %>?_method=DELETE" method="POST" style="display:inline;">
            <button type="submit" class="btn btn-danger mt-3" onclick="return confirm
            ('¿Estás seguro de que deseas eliminar este alumno?');">Eliminar Alumno</button>
        </form>
    </div>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>

</html>